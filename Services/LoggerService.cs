using Serilog;

namespace KozossegiAPI.Services
{
    public static partial class LoggerService {

        //internal static void LogStartupMessage(ILogger logger, string v)
        //{
        //    logger.Log(LogLevel.Information, v, "test");
        //}

        //[LoggerMessage(Level = LogLevel.Information, Message = "Authenticate user")]
        //static partial void LogStartupMessage(ILogger logger, string description);
    }
}
